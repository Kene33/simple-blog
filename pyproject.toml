[tool.pytest.ini_options]
pythonpath = ["."]
asyncio_mode = "auto"
markers = ["postgres: requires TEST_DATABASE_URL"]

[tool.ruff]
target-version = "py312"
line-length = 180

[tool.ruff.lint]
select = ["E", "F", "I"]
ignore = ["E501"]
